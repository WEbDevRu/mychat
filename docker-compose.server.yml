version: '2'
services:
  mongo:
    image: mongo:4.4.3
    restart: always
    entrypoint: [ "/usr/bin/mongod", "--bind_ip_all", "--replSet", "rs0" ]
    volumes:
      - mychat-mongo:/data/db
    expose:
      - 27017
    ports:
      - 57011:27017

  core:
    build:
      context: ./core
      dockerfile: Dockerfile
    environment:
      - COOKIE_SECRET=${COOKIE_SECRET}
      - TOKEN_SECRET=${TOKEN_SECRET}
      - CORS_ORIGIN=${CORS_ORIGIN}
      - MONGO_URL=${MONGO_URL}
    ports:
      - 26502:3001

  web-client:
    build:
      context: ./web-client
      dockerfile: Dockerfile.prod
    ports:
      - 26501:3000


volumes:
  mychat-mongo:

networks:
  default:
    external:
      name: bridge-mychat
